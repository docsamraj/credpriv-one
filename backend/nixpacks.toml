[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "cd ../shared && npm install",
  "npm install"
]

[phases.build]
cmds = [
  "cd ../shared && npm run build",
  "npx prisma generate",
  "npm run build"
]

[start]
cmd = "npx prisma migrate deploy && npm start"
