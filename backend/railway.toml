[build]
builder = "nixpacks"
buildCommand = "cd ../shared && npm install && npm run build && cd ../backend && npm install && npx prisma generate && npm run build"

[deploy]
startCommand = "npx prisma migrate deploy && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
