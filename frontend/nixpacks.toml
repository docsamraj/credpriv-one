[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "cd ../shared && npm install",
  "npm install"
]

[phases.build]
cmds = [
  "cd ../shared && npm run build",
  "npm run build"
]

[start]
cmd = "npm start"
