# Do not use — Railway Root Directory for this service must be / (repo root).
# This file exists only so a misconfigured "frontend" root fails with a clear message.
[phases.build]
cmds = ["echo 'Railway Root Directory must be / (repo root), not frontend. See RAILWAY.md' && exit 1"]
