# Nixpacks config when Railway Root Directory = frontend/
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --workspace=@credpriv/frontend --ignore-scripts"]

[phases.build]
cmds = ["rm -rf .next", "npm run build"]

[start]
cmd = "npm start"
