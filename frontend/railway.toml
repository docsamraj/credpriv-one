# Deprecated — use repo root + railway-frontend.toml (see RAILWAY.md)
# If Railway Root Directory is still "frontend", change it to "/" (repo root).

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.frontend"

[deploy]
healthcheckPath = "/login"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
