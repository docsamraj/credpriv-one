# Deprecated — Railway Root Directory MUST be / (repo root), not frontend.
# Use railway-frontend.toml at repo root instead.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/login"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
startCommand = "node server.js"
